import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  main: {
    display: 'flex',
    flexDirection: 'column',
  },
  button: {
    marginTop: '2px',
    marginBottom: '2px',
  },
});
